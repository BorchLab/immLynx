% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.extractChainSeqs}
\alias{.extractChainSeqs}
\title{Extract CDR3 sequences, barcodes, and optionally V/J genes}
\usage{
.extractChainSeqs(input, chains, combine_chains = FALSE, with_genes = FALSE)
}
\arguments{
\item{input}{A SingleCellExperiment object.}

\item{chains}{"TRB", "TRA", or "both".}

\item{combine_chains}{Logical. When chains="both", concatenate TRA and TRB
with "_" and keep only cells carrying both.}

\item{with_genes}{Logical. If TRUE, also return `v` and `j` gene vectors
aligned to `sequences`.}
}
\value{
A list with `sequences` and `barcodes`, plus `v` and `j` when
  `with_genes = TRUE`.
}
\description{
Shared chain-extraction logic for the neighbor-search and
  featurization wrappers. Returns one entry per cell that carries a usable
  sequence for the requested chain(s).
}
\keyword{internal}
